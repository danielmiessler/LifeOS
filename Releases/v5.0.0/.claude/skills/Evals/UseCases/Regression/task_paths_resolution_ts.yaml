# Task: Path Resolution Semantics — TypeScript
# Verifies hooks/lib/paths.ts and PAI/lib/paths.ts resolution against the
# two-domain config contract documented in
# PAI/DOCUMENTATION/PAISystemArchitecture.md "## Directory Structure".
#
# Auto-deprecate signal: if the architecture doc's directory-structure block
# ("## Directory Structure" block) changes to introduce/remove domains, this task must be revised.

id: task_paths_resolution_ts
description: "TS path lib resolves CLAUDE_CONFIG_DIR + PAI_DIR per architecture Directory Structure"
type: regression
domain: coding
source: refactor

graders:
  - type: binary_tests
    weight: 1.0
    params:
      test_files:
        - /Users/neil/src/Personal_AI_Infrastructure/Releases/v5.0.0/.claude/hooks/lib/paths.test.ts
      test_command: bun test
      timeout_ms: 30000

trials: 1
pass_threshold: 1.0

tags:
  - paths
  - configurability
  - regression
  - architecture-directory-structure

created_at: "2026-05-02"
